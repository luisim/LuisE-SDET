name: E2E Tests

on:
  push:
    branches: [ main, develop ]
    # Run tests on every push to main/develop
  pull_request:
    branches: [ main, develop ]
    # Run tests on every PR to main/develop
  workflow_dispatch: # Allows manual triggering from Actions tab

jobs:
  test:
    name: E2E Tests - ${{ matrix.browser }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        browser: [chromium]  # Start with just Chromium, add more browsers later
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Get npm cache directory
        id: npm-cache-dir-path
        shell: bash
        run: echo "dir=$(npm config get cache)" >> ${GITHUB_OUTPUT}
      
      - name: Cache npm dependencies
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: ${{ steps.npm-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-npm-${{ hashFiles('tests/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-
      
      - name: Install dependencies
        working-directory: ./tests
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi
      
      - name: Install Playwright Browsers
        run: |
          cd tests
          npx playwright install --with-deps ${{ matrix.browser }}
      
      - name: Run Playwright tests
        run: |
          cd tests
          npm test -- --project=${{ matrix.browser }}
        env:
          BASE_URL: https://luisim.github.io/LuisE-SDET/
        continue-on-error: false
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.browser }}-${{ matrix.os }}
          path: |
            tests/test-results/
            tests/playwright-report/
          retention-days: 30
      
      - name: Upload test videos
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-videos-${{ matrix.browser }}
          path: tests/test-results/**/*.webm
          retention-days: 7
      
      - name: Upload test screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-screenshots-${{ matrix.browser }}
          path: tests/test-results/**/*.png
          retention-days: 7

  test-report:
    name: Generate Test Report
    runs-on: ubuntu-latest
    needs: test
    if: always()
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Get npm cache directory
        id: npm-cache-dir-path
        shell: bash
        run: echo "dir=$(npm config get cache)" >> ${GITHUB_OUTPUT}
      
      - name: Cache npm dependencies
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: ${{ steps.npm-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-npm-${{ hashFiles('tests/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-
      
      - name: Download all test results
        uses: actions/download-artifact@v4
        with:
          pattern: test-results-*
          merge-multiple: true
          path: tests/test-results
      
      - name: Install dependencies
        run: |
          cd tests
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi
      
      - name: Setup Java for Allure
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
        continue-on-error: true
      
      - name: Generate HTML report
        run: |
          cd tests
          npm run test:report
        continue-on-error: true
      
      - name: Install Allure CLI
        run: |
          cd tests
          npm install -g allure-commandline || echo "Allure CLI install failed, continuing..."
        continue-on-error: true
      
      - name: Generate Allure report
        run: |
          cd tests
          if [ -d "allure-results" ] && [ "$(ls -A allure-results)" ]; then
            allure generate allure-results --clean -o allure-report || echo "Allure generation failed"
          else
            echo "No Allure results found, skipping report generation"
          fi
        continue-on-error: true
      
      - name: Upload HTML report
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: tests/playwright-report/
          retention-days: 30
      
      - name: Upload Allure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: tests/allure-report/
          retention-days: 30
        continue-on-error: true

  smoke-tests:
    name: Smoke Tests - Critical Path
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Get npm cache directory
        id: npm-cache-dir-path
        shell: bash
        run: echo "dir=$(npm config get cache)" >> ${GITHUB_OUTPUT}
      
      - name: Cache npm dependencies
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: ${{ steps.npm-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-npm-${{ hashFiles('tests/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-
      
      - name: Install dependencies
        run: |
          cd tests
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi
      
      - name: Install Playwright Browsers
        run: |
          cd tests
          npx playwright install --with-deps chromium
      
      - name: Run smoke tests
        run: |
          cd tests
          npm run test:smoke
        env:
          BASE_URL: https://luisim.github.io/LuisE-SDET/
      
      - name: Upload smoke test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-test-results
          path: tests/test-results/
          retention-days: 7

